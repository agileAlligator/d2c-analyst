# Database
DATABASE_URL=postgresql://d2c:d2c@localhost:5434/d2c

# LLM

OPENAI_API_KEY=sk-...

# Shopify (optional — only needed for live ingest, not for demo)
SHOPIFY_SHOP_DOMAIN=your-store.myshopify.com
SHOPIFY_ACCESS_TOKEN=shpat_...

# Meta Ads (optional — only needed for live ingest, not for demo)
META_ACCESS_TOKEN=EAAxxxxx...
META_AD_ACCOUNT_ID=act_xxxxxxxxx

# Shiprocket (optional — only needed for live ingest, not for demo)
SHIPROCKET_TOKEN=your-token-here

# API authentication (format: key:merchant_id,key2:merchant2)
# API_KEYS_RAW=your-secret-key:demo
# Allowed CORS origins (JSON array required by pydantic-settings)
# ALLOWED_ORIGINS=["http://localhost:10002"]
